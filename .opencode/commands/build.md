---
description: Build the NanoClaw TypeScript project
agent: build
---

Build the NanoClaw project by running `npm run build`. This compiles TypeScript to JavaScript in the `dist/` directory. Report any type errors or compilation failures. After a successful build, you may also want to run `npm run typecheck` to verify type safety.
