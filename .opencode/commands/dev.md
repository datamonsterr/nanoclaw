---
description: Start the NanoClaw dev server with hot reload
agent: build
---

Start the NanoClaw development server with hot reload using `npm run dev`. Watch for compilation or startup errors and report them. The server uses tsx for TypeScript execution.
